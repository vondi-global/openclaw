---
name: vondi-claude-sessions
description: "Управление Claude Code сессиями: просмотр, подключение и live-контроль через tmux. Позволяет продолжить работу с компьютера через Telegram."
metadata: { "openclaw": { "emoji": "🖥️", "requires": { "bins": ["claude-sessions"] } } }
---

# Claude Session Manager

Управление сессиями Claude Code — просмотр истории, подключение к нужной сессии, live-контроль через tmux.

## Ключевые понятия

- **Session ID** — UUID, например `45cc27c9-36ea-49b3-9af7-9c05296f6a10`
- **Короткий ID** — первые 8 символов: `45cc27c9`
- **Сессии хранятся** в `~/.claude/projects/<project>/<session-id>.jsonl`
- **Два режима подключения:**
  - **tmux-attach** — live routing через `tmux send-keys` (для активных сессий в tmux)
  - **resume-attach** — `claude --resume <id>` (для завершённых сессий из истории)

## КРИТИЧЕСКОЕ ПРЕДУПРЕЖДЕНИЕ: claude-sessions attach

`claude-sessions attach <id>` ПОСТОЯННО записывает session ID в sessions.json.
Все последующие сообщения бота идут через `claude --resume <id>` пока не будет вызван `detach`.

**НИКОГДА не делай attach к:**

- Сессии, которая сейчас активна (запущена в терминале/tmux) — вызовет зависание на 30 мин
- Незнакомым сессиям без проверки их статуса

**Для live-контроля активной сессии используй `tmux-attach`** (см. ниже).
**`attach` подходит ТОЛЬКО** для продолжения ЗАВЕРШЁННЫХ сессий из истории.

## Список сессий с кнопками (ОБЯЗАТЕЛЬНЫЙ формат для Telegram)

Когда пользователь просит показать сессии — ВСЕГДА выводи BUTTONS: директив с кнопками.
Это создаёт inline keyboard в Telegram — пользователь нажимает кнопку вместо ввода ID вручную.

Алгоритм:

1. Запусти `claude-sessions list --limit 8`
2. Проверь активные tmux сессии: `shared list`
3. Построй список кнопок: каждая строка = одна сессия
4. Включи в ответ BUTTONS: директив (НЕ message tool — используй именно этот формат)
5. `callback_data` каждой кнопки = `подключи <short_id>` (первые 8 символов session ID)

**Правила BUTTONS: директива:**

- Пишется на отдельной строке: `BUTTONS:` затем JSON массив
- Формат: массив рядов кнопок, каждый ряд = массив кнопок
- Максимум 1 кнопка в ряду для сессий (чтобы текст помещался)
- `callback_data` не длиннее 64 байт

**Пример ответа с кнопками:**

```
Последние сессии (нажми чтобы подключиться):

BUTTONS: [
  [{"text": "⚡ shared-2 | АКТИВНА | опиши стили маркетплейса", "callback_data": "подключи shared-2"}],
  [{"text": "45cc27c9 | vondi-global | реализуй migration 000006…", "callback_data": "подключи 45cc27c9"}],
  [{"text": "2c6f08cd | vondi-global | B2C GAP план",      "callback_data": "подключи 2c6f08cd"}],
  [{"text": "567b52fe | vondi-global | продолжи файл…",  "callback_data": "подключи 567b52fe"}],
  [{"text": "Отключить (вернуться в openclaw)",                    "callback_data": "отключи сессию"}]
]
```

Формат текста кнопки для tmux: `⚡ <tmux-name> | АКТИВНА | <описание>`
Формат текста кнопки для истории: `<short_id> | <проект> | <первое сообщение (30 символов)>`
Максимум 8 кнопок сессий + 1 кнопка "Отключить".

```bash
# Для получения данных:
claude-sessions list --limit 8
shared list   # активные tmux сессии
```

## Подключение к сессии — алгоритм выбора режима

```bash
# Шаг 1: Проверить есть ли активные tmux сессии с claude
shared list
# Если есть shared-2 и в ней запущен claude → использовать tmux-attach

# Шаг 2а: Активная tmux сессия → tmux-attach
claude-sessions tmux-attach shared-2

# Шаг 2б: Завершённая сессия из истории → resume-attach
claude-sessions resume-attach 45cc27c9

# Шаг 3: Проверить статус
claude-sessions proxy-status
```

## Управление proxy-сессиями

```bash
# Подключить активную tmux сессию
claude-sessions tmux-attach shared-2
claude-sessions tmux-attach shared-2 --label "разработка auth"

# Подключить завершённую сессию (claude --resume)
claude-sessions resume-attach 45cc27c9
claude-sessions resume-attach 45cc27c9-36ea-49b3-9af7-9c05296f6a10

# Проверить текущее состояние
claude-sessions proxy-status

# Отключить proxy (вернуться в openclaw)
claude-sessions proxy-detach
```

**После подключения**: все сообщения пользователя в Telegram автоматически маршрутизируются в выбранную сессию. Openclaw agent НЕ обрабатывает эти сообщения.

**Команды управления (пользователь пишет в Telegram):**

- `назад` / `back` / `вернись` → возврат в openclaw
- `экран` / `screen` / `что там` → посмотреть текущий экран tmux

## Просмотр tmux сессии (без подключения)

```bash
# Посмотреть что сейчас на экране
shared capture 2

# Отправить команду не переключая режим
shared send 2 "покажи статус"
```

## Информация о сессии

```bash
# По короткому ID (первые 8 символов)
claude-sessions info 45cc27c9

# По полному UUID
claude-sessions info 45cc27c9-36ea-49b3-9af7-9c05296f6a10
```

Выводит: проект, дата, количество сообщений, последние 5 сообщений.

## Запущенные процессы

```bash
# Все запущенные процессы claude + tmux сессии
claude-sessions running
```

## Устаревший механизм attach (только для openclaw agent sessions)

```bash
# Подключить через openclaw sessions.json (НЕ для активных tmux сессий!)
claude-sessions attach 45cc27c9

# Отключить
claude-sessions detach
```

## Типовые сценарии

### Сценарий 1: Управлять активной tmux сессией из Telegram

```bash
# На компьютере: запустить claude в tmux
shared start 2
shared attach 2
# Внутри: запустить claude

# Из Telegram через бота:
claude-sessions tmux-attach shared-2
# Теперь все сообщения идут в shared-2
# Написать "назад" чтобы вернуться
```

### Сценарий 2: Продолжить вчерашнюю сессию

```bash
# Показать сессии
claude-sessions list --limit 8

# Подключить нужную
claude-sessions resume-attach 45cc27c9

# Теперь сообщения идут через claude --resume
# Написать "назад" чтобы вернуться
```

### Сценарий 3: Убить зависший процесс

```bash
# Узнать PID
claude-sessions running

# Убить
kill 12345
```

## Совет: запускай claude через shared tmux

```bash
shared start 1   # создать сессию
shared attach 1  # войти
# Запустить claude внутри
```

Тогда бот может в любой момент подключиться через `tmux-attach shared-1`.
