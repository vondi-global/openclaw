---
name: vondi-claude-sessions
description: "Управление Claude Code сессиями: просмотр, подключение и live-контроль через tmux. Позволяет продолжить работу с компьютера через Telegram."
metadata: { "openclaw": { "emoji": "🖥️", "requires": { "bins": ["claude-sessions"] } } }
---

# Claude Session Manager

Управление сессиями Claude Code — просмотр истории, подключение к нужной сессии, live-контроль через tmux.

## Ключевые понятия

- **Session ID** — UUID, например `45cc27c9-36ea-49b3-9af7-9c05296f6a10`
- **Короткий ID** — первые 8 символов: `45cc27c9`
- **Сессии хранятся** в `~/.claude/projects/<project>/<session-id>.jsonl`
- **Бот подключается** через `claude-sessions attach <id>` — следующее сообщение идёт в ту сессию

## КРИТИЧЕСКОЕ ПРЕДУПРЕЖДЕНИЕ: claude-sessions attach

`claude-sessions attach <id>` ПОСТОЯННО записывает session ID в sessions.json.
Все последующие сообщения бота идут через `claude --resume <id>` пока не будет вызван `detach`.

**НИКОГДА не делай attach к:**

- Сессии, которая сейчас активна (запущена в терминале/tmux) — вызовет зависание на 30 мин
- Незнакомым сессиям без проверки их статуса
- Сессиям которые завершились с ошибкой

**ВМЕСТО attach для live-контроля используй tmux** (см. раздел ниже).
**attach подходит ТОЛЬКО** для продолжения ЗАВЕРШЁННЫХ сессий из истории.

## Список сессий с кнопками (ОБЯЗАТЕЛЬНЫЙ формат для Telegram)

Когда пользователь просит показать сессии — ВСЕГДА выводи BUTTONS: директив с кнопками.
Это создаёт inline keyboard в Telegram — пользователь нажимает кнопку вместо ввода ID вручную.

Алгоритм:

1. Запусти `claude-sessions list --limit 8`
2. Построй список кнопок: каждая строка = одна сессия
3. Включи в ответ BUTTONS: директив (НЕ message tool — используй именно этот формат)
4. `callback_data` каждой кнопки = `подключи <short_id>` (первые 8 символов session ID)

**Правила BUTTONS: директива:**

- Пишется на отдельной строке: `BUTTONS:` затем JSON массив
- Формат: массив рядов кнопок, каждый ряд = массив кнопок
- Максимум 1 кнопка в ряду для сессий (чтобы текст помещался)
- `callback_data` не длиннее 64 байт

**Пример ответа с кнопками:**

```
Последние сессии (нажми чтобы подключиться):

BUTTONS: [
  [{"text": "45cc27c9 | vondi-global | в клауде знают...", "callback_data": "подключи 45cc27c9"}],
  [{"text": "2c6f08cd | vondi-global | B2C GAP план",      "callback_data": "подключи 2c6f08cd"}],
  [{"text": "567b52fe | vondi-global | продолжи файл...",  "callback_data": "подключи 567b52fe"}],
  [{"text": "Отключить (новая сессия)",                    "callback_data": "отключи сессию"}]
]
```

Формат текста кнопки: `<short_id> | <проект> | <первое сообщение (30 символов)>`
Максимум 8 кнопок сессий + 1 кнопка "Отключить".

```bash
# Для получения данных:
claude-sessions list --limit 8
```

## Информация о сессии

```bash
# По короткому ID (первые 8 символов)
claude-sessions info 45cc27c9

# По полному UUID
claude-sessions info 45cc27c9-36ea-49b3-9af7-9c05296f6a10
```

Выводит: проект, дата, количество сообщений, последние 5 сообщений.

## Подключение к сессии

```bash
# Подключить — следующее сообщение в боте продолжит эту сессию
claude-sessions attach 45cc27c9

# Отключить — бот начнёт новую сессию
claude-sessions detach
```

**После `attach`**: следующее сообщение пользователя в Telegram/боте будет отправлено в эту сессию через `claude --resume <session-id>`. Полный контекст сохранён.

## Запущенные процессы

```bash
# Все запущенные процессы claude + tmux сессии
claude-sessions running
```

## Live-контроль через tmux (если claude запущен в tmux)

### Запуск claude в tmux (делать ЭТО на компьютере)

```bash
# Новая сессия в текущей директории
claude-tmux

# С именем
claude-tmux -s mywork

# Resume существующей сессии
claude-tmux -r 45cc27c9-36ea-49b3-9af7-9c05296f6a10
```

Имя tmux сессии будет вида `cls-vondi-global-1430`.

### Просмотр что происходит в tmux сессии

```bash
# Последние 20 строк экрана
tmux capture-pane -t cls-vondi-global-1430 -p | tail -20

# Весь scrollback
tmux capture-pane -t cls-vondi-global-1430 -p -S -

# Проверить все claude-сессии разом
for s in $(tmux list-sessions -F "#{session_name}" 2>/dev/null | grep "^cls-"); do
  echo "=== $s ==="
  tmux capture-pane -t "$s" -p 2>/dev/null | tail -5
done
```

### Отправить команду в tmux сессию

```bash
# Отправить текст + Enter
tmux send-keys -t cls-vondi-global-1430 -l -- "исправь баг в auth.go"
sleep 0.1
tmux send-keys -t cls-vondi-global-1430 Enter

# Ctrl+C (прервать)
tmux send-keys -t cls-vondi-global-1430 C-c

# Одобрить Yes/No запрос
tmux send-keys -t cls-vondi-global-1430 'y' Enter
```

### Список tmux сессий

```bash
tmux list-sessions
claude-tmux --list
```

## Типовые сценарии

### Сценарий 1: Продолжить работу с телефона

Пользователь пишет "покажи сессии" или "последние сессии":

1. `claude-sessions list --limit 8`
2. Отправить через `message` tool с кнопками (см. раздел "Список сессий с кнопками")
3. Пользователь нажимает кнопку → приходит "подключи 45cc27c9"
4. СНАЧАЛА проверь статус сессии: `claude-sessions info 45cc27c9`
   - Если сессия активна (есть tmux) → используй tmux send-keys (сценарий 2)
   - Если сессия завершена → выполнить `claude-sessions attach 45cc27c9`
5. Ответить: "Подключено. Следующее сообщение продолжит сессию [проект] от [дата]"
6. ВАЖНО: После работы выполни `claude-sessions detach` иначе все сообщения будут идти туда навсегда

### Сценарий 2: Контролировать живой процесс

Claude работает на компьютере в tmux. Хочешь видеть что происходит и слать команды:

```bash
# 1. Узнать имя tmux сессии
claude-sessions running

# 2. Посмотреть текущий экран
tmux capture-pane -t cls-vondi-1430 -p | tail -30

# 3. Отправить команду
tmux send-keys -t cls-vondi-1430 -l -- "остановись и покажи статус"
sleep 0.1
tmux send-keys -t cls-vondi-1430 Enter
```

### Сценарий 3: Убить зависший процесс

```bash
# Узнать PID
claude-sessions running

# Убить
kill 12345

# Или через tmux
tmux kill-session -t cls-vondi-1430
```

### Сценарий 4: Переключиться между проектами

```bash
# Список сессий с фильтром
claude-sessions list --project listings

# Подключить другую сессию
claude-sessions attach abc12345

# Проверить что подключено
claude-sessions list  # звёздочка покажет активную
```

## Совет: всегда запускай claude через claude-tmux

Добавь алиас в `~/.zshrc`:

```bash
alias cl='claude-tmux'
```

Тогда все сессии автоматически попадают в tmux и доступны через бота для live-контроля.
